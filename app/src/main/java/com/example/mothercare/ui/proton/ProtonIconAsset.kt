package com.example.mothercare.ui.proton

import com.example.mothercare.R

enum class ProtonIconAsset(
    var iconRes: Int,
    var contentDiscription: String
) {
    Map(iconRes = R.drawable.map,
        contentDiscription = "")

}